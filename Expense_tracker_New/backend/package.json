{
  "name": "ledger-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-mediated API for Ledger's multi-tenant SaaS backend (API Gateway + Lambda + Aurora Serverless v2 + Cognito).",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.600.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.1116.0",
    "@aws-sdk/client-rds-data": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0",
    "aws-jwt-verify": "^4.0.1",
    "pdf-lib": "^1.17.1",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "pg": "^8.12.0"
  },
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local": "sam local start-api",
    "test": "COGNITO_USER_POOL_ID=us-east-1_00000000000 COGNITO_CLIENT_ID=test-client-id node --test test/*.test.js"
  }
}
